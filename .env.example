# WhatSignal Environment Configuration

# Data directory for Docker volumes (default: ./data)
# This allows you to customize where all persistent data is stored
WHATSIGNAL_DATA_DIR=/mnt/data

# WhatsApp API Configuration
WHATSAPP_API_KEY=your-whatsapp-api-key-here
WHATSIGNAL_WHATSAPP_WEBHOOK_SECRET=your-webhook-secret-32-chars-min

# Encryption Configuration
WHATSIGNAL_ENABLE_ENCRYPTION=true
WHATSIGNAL_ENCRYPTION_SECRET=your-encryption-secret-32-chars-min

# Optional: Override API URLs for external access
# WHATSAPP_API_URL=http://external-waha-host:3000
# SIGNAL_RPC_URL=http://external-signal-host:8081

# Optional: Database path override
# DB_PATH=/custom/path/to/database.db

# Optional: Media cache directory override
# MEDIA_DIR=/custom/path/to/media/cache

# Environment setting (development/production)
WHATSIGNAL_ENV=production