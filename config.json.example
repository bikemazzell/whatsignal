{
  // WhatsApp configuration
  // - apiBaseUrl: URL of your Waha instance (WhatsApp HTTP API)
  // - apiKey: API key for authenticating with WAHA (must match WAHA's WHATSAPP_API_KEY)
  // - sessionName: Name of the WhatsApp session (used in API endpoints)
  // - webhookSecret: Secret for authenticating incoming webhooks
  // - messageTypingDelay: Milliseconds per character for typing simulation (max 3000ms)
  // - pollIntervalSec: How often to poll for messages if webhooks are not available
  "whatsapp": {
    "apiBaseUrl": "http://localhost:3000",
    "apiKey": "your-api-key",
    "sessionName": "default",
    "webhookSecret": "your-webhook-secret",
    "messageTypingDelay": 50,
    "pollIntervalSec": 30,
    "mediaConfig": {
      "maxSizeMB": {
        "image": 5,
        "video": 100,
        "document": 100,
        "voice": 16
      },
      "allowedTypes": {
        "image": ["jpg", "jpeg", "png"],
        "video": ["mp4"],
        "document": ["pdf", "doc", "docx"],
        "voice": ["ogg"]
      }
    }
  },

  // Signal configuration
  // - rpcUrl: URL of your signal-cli JSON-RPC daemon
  // - authToken: Authentication token for Signal API access
  "signal": {
    "rpcUrl": "http://localhost:8080",
    "authToken": "your-signal-auth-token"
  },

  // Message retry configuration
  // - initialBackoffMs: Initial delay before first retry
  // - maxBackoffMs: Maximum delay between retries
  // - maxAttempts: Maximum number of retry attempts
  "retry": {
    "initialBackoffMs": 1000,
    "maxBackoffMs": 60000,
    "maxAttempts": 5
  },

  // Number of days to keep message history
  "retentionDays": 30,

  // Logging level (debug, info, warn, error)
  "logLevel": "info"
} 